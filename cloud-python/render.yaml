# Render.com blueprint — one-click deploy of the Nickland Edusoft cloud.
# Docs: https://render.com/docs/blueprint-spec
services:
  - type: web
    name: nickland-edusoft-cloud
    runtime: docker
    rootDir: cloud-python
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    plan: starter
    autoDeploy: true
    envVars:
      # Both are mandatory — the service refuses to boot without them.
      # Do NOT regenerate PORTAL_SECRET on later deploys: it signs parent
      # session tokens, so changing it signs every parent out.
      - key: PORTAL_SECRET
        generateValue: true          # signs parent session tokens
      - key: DATABASE_URL
        sync: false                  # paste your Neon connection string (with ?sslmode=require)
      - key: WEB_CONCURRENCY
        value: "2"
