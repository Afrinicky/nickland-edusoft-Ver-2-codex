fastapi>=0.111
uvicorn[standard]>=0.30
psycopg[binary]>=3.2
psycopg_pool>=3.2
# Staff sign-in: teacher passwords are bcrypt on the desktop, and are verified
# here as-is so nobody has to be re-enrolled to work off-LAN.
bcrypt>=4.1

# The online system holds every school's full database, so the service needs
# the same driver the thin-cloud store uses — listed once, above.

# Signing the offline licences the desktop runs on (app/billing/licence.py).
# Ed25519: the private key signs here and never leaves this service; the
# desktop carries only the public key, which can check a lease and not make
# one. A deployment without this library still boots and still serves every
# school — it loses offline licensing and says so in the boot report.
cryptography>=41
